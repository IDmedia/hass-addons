version: '3'

services:
  hass-backup:
    container_name: hass-backup
    restart: always
    build: ./
    ports:
      - '8070:80'
    environment:
      - TZ=Europe/Oslo
    volumes:
      - '/volume2/docker/hass-backup/app:/app'
    image: hass-backup:latest